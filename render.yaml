services:
  - type: web
    name: wechat-dashboard-api
    rootDir: data_visualization/backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    plan: free
